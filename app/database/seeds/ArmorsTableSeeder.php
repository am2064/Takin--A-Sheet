<?php

class ArmorsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('armors')->truncate();

		$armors = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('armors')->insert($armors);
	}

}
